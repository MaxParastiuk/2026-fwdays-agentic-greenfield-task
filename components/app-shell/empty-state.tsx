export function EmptyState() {
  return (
    <div className="mb-8 max-w-[640px]">
      <h1
        className="font-[family-name:var(--font-display)] text-[2.375rem] font-medium leading-[1.08] tracking-[var(--tracking-tight)] text-[var(--text-strong)]"
      >
        A cover letter worth signing.
      </h1>
      <p className="mt-3 text-[15.5px] leading-[1.55] text-[var(--text-body)]">
        Provide your CV and the job posting. A Maker writes the letter, a
        Checker scores it against both, and the loop repeats up to three times —
        until the words fit the role.
      </p>
    </div>
  );
}
