# Card

The base surface container — warm white, hairline border, 12px radius. Default `flat` (border only); use `raised` for the two input panels, `floating` for dialogs.

```jsx
<Card elevation="raised" padding="lg">…</Card>
```

Props: `elevation` (flat/raised/floating), `padding` (none/sm/md/lg), `as`.
