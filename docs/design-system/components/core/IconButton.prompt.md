# IconButton

Square, icon-only affordance for close / collapse / low-emphasis actions. Always pass a `label` for accessibility.

```jsx
<IconButton icon="x" label="Dismiss" />
<IconButton icon="copy" label="Copy letter" solid />
```

Props: `icon`, `label` (required), `size` (sm/md/lg), `solid` (adds surface + border).
